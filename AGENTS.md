# AGENTS.md

Постоянные инструкции проекта для локального нотариального агента.

Этот файл задает проектные правила и порядок работы. Он не заменяет технические ограничения runtime, sandbox и системную политику безопасности.

## Назначение

- работать по нотариальному workflow пользователя;
- не терять проектные правила между сессиями;
- не подменять ручной процесс произвольной импровизацией;
- сохранять единый формат Оглавления, Приказа и итоговых файлов.
- агенту запрещено завершать задачу по внутреннему ощущению достаточности; завершение допускается только после буквальной проверки against user request: все требуемые действия выполнены, все обещанные шаги закрыты, все запрошенные результаты реально получены; любая подмена полного результата частичным, техническим, предварительным или "достаточным" результатом запрещена.

## Источники Истины

Использовать источники в таком порядке:

1. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\AGENTS.md`
2. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\PROJECT_STATE.md`
3. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\workflow\manual-workflow.md`
4. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\master prompt\Промпт по поиску документов 18.md`
5. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\workflow\Утверждаю.md`
6. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\order\Текст приказа 18  15.11.10..md` как шаблон Приказа. Рабочие приказы по подтемам хранятся в `input\output ready\Тема-<N>-...\02-orders\<subtopic_id>.md
7. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\workflow\User and LLM Interaction in LLM Contest Window.md`
8. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\workflow\outline overrides\<номер темы>.md` при наличии
9. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\output examples\15.11.10. Наследование имущества.md`
10. `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\output examples\15.11.10. Наследование имущества.docx`

## Роли Файлов

- `manual-workflow.md` = компас процесса.
- `Промпт по поиску документов 18.md` = стратегия поиска, верификации и формата.
- `Текст приказа ... .md` = тактика, операционное руководство и поэтапное исполнение.
- `User and LLM Interaction in LLM Contest Window.md` = эталон механики и стиля работы в окне LLM.
- `PROJECT_STATE.md` = текущее оперативное состояние проекта.
- `outline overrides` = утвержденное Оглавление, если оно уже согласовано.

## Порядок Работы при начале работы с новой Темой

Агент обязан работать так:

1. Прочитать `AGENTS.md`.
2. Прочитать `PROJECT_STATE.md`.
3. Прочитать `manual-workflow.md`.
4. Прочитать мастер-промпт `Промпт по поиску документов 18`.
5. Прочитать `Утверждаю.md`.
6. Найти основную тему, указанную пользователем.
7. Сформировать Оглавление по теме.
8. Прочитать шаблон приказа.
9. Создать копии приказа под конечные подпункты темы.
10. Прочитать guide по взаимодействию в окне LLM.
11. Для каждой конечной подтемы подготовить `run-subtopic` workspace.
12. Использовать `04-web-plan` как обязательный web-first слой Части 2.
13. Выполнять рабочий цикл по подтемам.

Рабочий режим по Оглавлению двухэтапный:

1. `draft-main-theme-outline`:
   - подготовить draft Оглавления;
   - подготовить review-файл;
   - остановиться до рендеринга приказов.
2. `prepare-main-theme`:
   - использовать approved outline;
   - только после этого строить копии приказов и execution packets.

## Порядок Работы при начале работы с новой Подтемой

При старте каждой новой сессии работы с новой Подтемой, агент обязан самостоятельно прочитать `AGENTS.md`, `PROJECT_STATE.md`, `manual-workflow.md`, мастер-промпт `Промпт по поиску документов 18` и текст соответствующего Подтеме Приказа. Приказы хранятся в `input\output ready\Тема-<N>-...\02-orders\<subtopic_id>.md`. 
Агент находит папку `02-orders` внутри папки текущей большой темы и читает файл по номеру подтемы (например, `16.9.2.md`). 
И без вопросов к пользователю доложить: текущая тема, завершённые подтемы, следующая подтема, состояние её workspace и конкретный первый шаг. 
После доклада агент ждёт команды пользователя и не начинает работу без неё.

## Правила Оглавления

- Оглавление нельзя дробить механически.
- Формулировки подпунктов должны сохранять юридический смысл исходной темы.
- Подпункты про тариф нельзя оставлять в виде голой строки `Исчисление размера федерального и регионального тарифа`.
- Тарифный подпункт должен быть достроен по смыслу конкретной подтемы.
- Если одна исходная подтема распадается на несколько конечных подпунктов, использовать нумерацию вида `16.1.1`, `16.1.2`, `16.1.3`.
- Если по теме уже существует утвержденное Оглавление в `input\workflow\outline overrides\<номер темы>.md`, оно имеет приоритет над автогенерацией.
- При наличии approved outline агент обязан строить копии приказов, execution packets и дальнейшее исполнение по нему.
- Если approved outline отсутствует, агент не должен переходить к рендерингу приказов; он обязан остановиться на стадии draft/review.

## Правила Приказа

- Исходный шаблон приказа не изменять.
- Для каждой конечной подтемы создавать отдельную копию приказа.
- В копии приказа изменяются:
  - Раздел III. ОГЛАВЛЕНИЕ
  - наименование текущей конечной подтемы во всех релевантных местах 11 Частей
- Остальной текст приказа сохраняется.
- Для имен файлов использовать короткий формат по номеру подтемы, например `16.1.2.md`.

## Правила Исполнения

- Работа идет не одним большим ответом, а по логике Частей приказа.
- До завершения Части 1 и сигнала `GO/СТАРТ` нельзя начинать поиск документов.
- После `GO/СТАРТ` агент должен воспроизводить поведенческий ритм из `User and LLM Interaction in LLM Contest Window.md`.
- Перед реальным исполнением Части 2 агент должен использовать подготовленный `04-web-plan`: `queries`, `source-cascade`, `research-pack`, `core-template`, `research-log`, `evidence`.
- `part-02.launch-packet` должен быть максимально буквальным продолжением ручного сильного режима, а не пересказом мастер-промпта агентом.
- После захвата каждой Части агент обязан пересобрать динамические пакеты для следующих Частей, чтобы не терять живой контекст уже найденных материалов.
- Любая функция захвата или обработки вывода LLM обязана иметь соответствующую валидацию в `validate_part_output()`. Функция без валидации считается незавершённой. Части `6–9` валидируются по тем же правилам карточек, что и Части `4–5`.

### Канонические заголовки Частей

Агент обязан использовать следующие канонические заголовки для каждой Части. Переименование, укрупнение или пропуск заголовков запрещено.

| Часть | Канонический заголовок |
|---|---|
| Часть 2 | `**ЧАСТЬ 2. АНАЛИЗ ОБЛАСТИ ПРАВА**` |
| Часть 3 | `**ЧАСТЬ 3. БЛОКИ ПОИСКА СФЕР ПРАВА (I–XXXVII)**` |
| Часть 4 | `**ЧАСТЬ 4. ПОИСК ПО ОРГАНАМ ВЛАСТИ**` |
| Часть 5 | `**ЧАСТЬ 5. СЛОЙНОСТЬ**` |
| Часть 6 | `**ЧАСТЬ 6. ФИЛЬТР**` |
| Часть 7 | `**ЧАСТЬ 7. ФИЛЬТР БЕЗ ПОВТОРЕНИЙ**` |
| Часть 8 | `**ЧАСТЬ 8. ФИЛЬТР БЕЗ ПОВТОРЕНИЙ ФЕДЕРАЛЬНЫХ ДОКУМЕНТОВ**` |
| Часть 9 | `**ЧАСТЬ 9. ДЕЛЬТА-АУДИТ**` |
| Часть 10 | `**ЧАСТЬ 10. РАСШИРЕННЫЙ МИНИ-КОНСПЕКТ**` |
| Часть 11 | `**ЧАСТЬ 11. ПЕРЕЧЕНЬ ПРАКТИЧЕСКИХ ЗАДАНИЙ ПО ЗАДАННОЙ ТЕМЕ**` |

## Запрет прямой записи в stage-outputs

- Прямая запись или редактирование файлов в `02-stage-outputs/` ЗАПРЕЩЕНА.
- Единственный разрешённый способ записи результата Части — команда `capture-part-output`.
- Использование `apply_patch`, `write_file` или любого другого инструмента для записи
  в `02-stage-outputs/` напрямую является ошибкой orchestration и делает часть untrusted.
- Untrusted части блокируют публикацию.
- Для Частей `2–11` агент обязан использовать `04-web-plan\reasoning-layer` как отдельный reasoning contract, а не только механически передавать текст Приказа.
- Для Частей `6–11` агент обязан использовать `04-web-plan\follow-up-parts`, а не только сырые `stage-inputs`, если нужен self-contained packet для LLM/API.
- Для Частей `6–9` follow-up пакет обязан запускать новый внешний поиск по текущей задаче и не сводить результат к сухому `новых документов не выявлено` без повторного прохода.
- Для Части `3` (БЛОКИ ПОИСКА СФЕР ПРАВА): каждый блок I–XXXVII, по которому найдены документы, оформляется карточками с полным набором полей включая `Значение для темы`. Блоки где документы не найдены — отмечаются строкой `Статус: не найдено` без URL и без карточки. Запрещено ставить ссылки на блоки где ничего не найдено — пустая ссылка хуже честного «не найдено».
- Для Части `5` (СЛОЙНОСТЬ) обязательны полноценные карточки документов с полями `Вид документа`, `Полное наименование`, `Структурный элемент`, `URL1`, `URL2`, `VERIFIED URL2:`, `Заголовок страницы URL2`, `Сверка реквизитов`, `Значение для темы`. Inline-список с URL в одну строку (формат `1. Название. URL1: ...; URL2: ...`) запрещён — он не проходит валидацию и даёт нулевые метрики верификации.
- Для Части `10` агент обязан давать мини-конспект с документами-опорами карточками.
- Для Части `10` мини-конспект должен содержать не более `10` пунктов.
- Для Части `11` агент обязан держаться формулировки Приказа: только 4-я колонка дневника, только перечень выполненных стажером действий по характеру задания, без перечисления документов.
- Текст карточек документов, аналитических разделов и мини-конспекта должен быть содержательным и юридически конкретным; агент не должен выдавать сухой, формальный или механически сжатый результат; по теме обязан быть предоставлен объективно более полный и насыщенный материал.
- Для каждого документа поле `Значение для темы` должно раскрывать **прямую нормативную связь** данного акта с нотариальным действием по подтеме: устанавливает ли он полномочие нотариуса на совершение данного действия, основания и условия его совершения, требования к форме или реквизитам составляемого документа, порядок проверки предоставляемых документов и сведений, основания для отказа в совершении нотариального действия, размер нотариального тарифа (федерального или регионального), ответственность нотариуса либо иной элемент нотариального производства, непосредственно относящийся к данной подтеме. Если такой прямой нормативной связи нет — документ не включается.
- Запрещено: `Значение для темы: документальное оформление доказательств по спорам о защите прав потребителей.` — это однострочная отписка без юридического содержания. Требуется: что конкретно в этой статье, как именно это применяется нотариусом при данном нотариальном действии, какой практический вывод.
- Запрещено использовать формулировки `регулирует данную сферу`, `имеет значение для темы`, `относится к предмету регулирования` без немедленной конкретизации: обязательно указать, какой именно элемент нотариального производства по данной подтеме регулирует эта норма.
- **СТРОГОЕ ПРАВИЛО ДЛЯ ВСЕХ ЧАСТЕЙ 2–11 БЕЗ ИСКЛЮЧЕНИЯ.** Перед включением любого документа в любую Часть агент обязан пройти тест: *«Указанный структурный элемент прямо регулирует действие нотариуса или документ, составляемый нотариусом по данной подтеме?»* Если ответ **нет** — документ не включается. Если ответ **косвенно** — документ помещается в КАРАНТИН с явным указанием причины. Логика «тема → нотариус что-то делает → значит эта статья подходит» без прямого нормативного основания в тексте самой статьи — запрещена.
- Поле `Значение для темы` обязано строиться по структуре: **(1) что статья реально говорит** (по тексту нормы) → **(2) прямая связь с нотариальным действием по подтеме** (только если она есть в тексте статьи или вытекает из неё однозначно). Писать "Значение для темы" которое описывает нотариальное действие, не упомянутое в самой статье — запрещено.
- Мини-конспект должен быть развернутым и опорным: не пересказ “в общем”, а последовательное изложение ключевых правовых положений с практическими акцентами и с документами-подтверждениями после каждого смыслового пункта.
- Если по теме доступны дополнительные релевантные акты, редакционные изменения, цифровой, специальный субъектный, международный или процессуальный слой, агент не должен пересушивать результат ради краткости; полнота и содержательная плотность имеют приоритет над искусственным сжатием ответа.
- Для тарифных подпунктов web-first поиск и финальная формулировка должны сохранять привязку к базовой подтеме группы.
- Follow-up этапы не должны ломать уже собранную структуру ответа.
- Доборы и дельта-аудит выполняются без повторения уже выданных документов в рамках текущей темы.

## Режим Экономии Контекста

- По умолчанию агент работает в `lean-mode`: экономит токены на служебной обвязке, но не имеет права экономить на качестве финального документа, ширине охвата, плотности аналитики, количестве релевантных документов и полноте Частей `2–10`.
- Базовый принцип lean-mode: экономия допускается только на orchestration-слое; substantive-корпус, количество уникальных документов и плотность анализа не сокращаются ради токенов.
- Контроль токеноемкости вести по факту, а не по ощущениям: на одну подтему держать модель `один короткий старт -> один основной проход -> один короткий финал`; не плодить промежуточные сообщения без блокера; не перечитывать большие канонические файлы целиком без необходимости; не создавать тяжелые run-артефакты по умолчанию; после завершения подтемы проверять только минимум: не было ли лишних крупных прогонов, повторного подъема большого контекста, служебных файлов сверх минимума и повторного цикла из-за обвязки, а не из-за реального содержательного добора.
- Агенту запрещено длинно пересказывать пользователю уже данные инструкции, мастер-промпт, Приказ, `AGENTS.md` и `PROJECT_STATE.md`, если пользователь прямо не запросил такой пересказ.
- При старте новой подтемы агент дает только краткий операционный доклад: текущая тема, завершенные подтемы, следующая подтема, состояние workspace, первый шаг. Без расширенных объяснений сверх этого минимума.
- В ходе исполнения агент не должен засорять чат промежуточными сообщениями о штатных технических действиях, если нет блокера, конфликта правил или необходимости получить решение пользователя.
- Финальный ответ пользователю должен быть кратким и содержать только результат, ключевые метрики, канонические пути и действительно важные оговорки.
- Агент не должен повторно читать большие канонические файлы целиком без необходимости, если они не менялись. Для повторной сверки агент обязан по возможности использовать точечное чтение нужных разделов, поиск по заголовкам и локальные выборки вместо полной повторной загрузки файла в контекст.
- Если постоянные правила не менялись, агент не должен заново пересказывать их в рабочие run-файлы, launch-packets и служебные заметки.
- Для одной подтемы агент обязан стремиться к модели `один сильный end-to-end прогон`, а не к цепочке многословных промежуточных прогонов, если это не требуется для исправления реального дефекта.
- Агенту запрещено создавать дублирующие промежуточные `.md`-файлы, если тот же рабочий результат можно безопасно вести в одном основном рабочем файле или в минимальном наборе служебных файлов.
- Карточки документов, уже созданные в рамках текущей подтемы, не должны многократно переписываться заново в служебных файлах без содержательного прироста.
- Метрики, page-check, дедупликация, техническая сборка, сравнение версий, поиск повторов и иные механические операции должны по максимуму выноситься в локальные скрипты и утилиты, а не в токеноемкие текстовые прогоны модели.
- Агенту запрещено заливать большие substantive-блоки одним `apply_patch`; длинные части и крупные фрагменты текста вносятся только порционно, малыми секциями, без промежуточных сообщений о техническом дроблении.
- Экономия токенов допускается только за счет сокращения служебного шума, повторов, лишних сообщений, дублирующих файлов и повторной загрузки неизменившегося канона. Экономия за счет сокращения документного корпуса, правового анализа, follow-up поиска, карточек и содержательной насыщенности запрещена.
- Режим предельно низкого токенопотребления обязателен и понимается только в следующем смысле: агент сокращает исключительно служебную обвязку и ничего больше. Под служебной обвязкой для целей настоящего правила понимаются только: сообщения в чат, промежуточные доклады, повторное чтение неизменившихся канонических файлов, дублирующие временные файлы, лишние промежуточные сборки, лишние ретраи, ручные механические перепроверки, которые могут быть выполнены локальными скриптами, а также иные чисто технические orchestration-действия, не влияющие на substantive-результат. Агенту запрещается по собственному усмотрению относить к служебной обвязке поиск, добор новых документов, карточки документов, follow-up поиск для Частей `6–9`, аналитические выводы, мини-конспект, дельта-аудит, ширину охвата, количество правовых узлов, метрики итогового файла, page count и любые иные элементы substantive-слоя. Дисциплина исполнения обязательна: один короткий старт, один тихий основной прогон, один короткий финал; писать в чат между ними запрещено, кроме случаев реального блокера, прямо предусмотренного `AGENTS.md`. Агенту запрещается оправдывать дополнительные действия, сообщения, остановки, повторные прогоны или сокращение substantive-слоя ссылками на собственное понимание lean-mode, экономии токенов, удобства, разумности, достаточности, оптимизации, интерпретации правил или якобы технической необходимости, если такая необходимость прямо не подтверждена реальным блокером. Любое действие, в результате которого уменьшены количество новых релевантных документов, ширина поиска, насыщенность аналитики, полнота Частей `2–10`, качество document corpus, метрики итогового файла или page count, считается не экономией токенов, а нарушением режима исполнения. Все сомнения в применении настоящего правила толкуются против сокращения substantive-слоя и против любых дополнительных сообщений в чат.
- С момента отдельного указания пользователя действует дополнительный жесткий лимит исполнения: агенту запрещено тратить более `2%` токенов на выдачу готового результата по одной теме или подтеме. Этот лимит может соблюдаться только за счет сокращения служебной обвязки; все требования к качеству, метрикам, ширине поиска, количеству новых релевантных документов, плотности аналитики, полноте Частей `2–10`, качеству document corpus и page count подлежат полному исполнению без снижения.
- Для каждой новой подтемы содержательный корпус Частей `2–11` должен собираться с нуля в рамках собственного нового `run`. Агенту запрещено брать substantive-текст предыдущей или соседней подтемы как базовый текст для новой подтемы, даже если темы близки по предмету.
- Допустимое переиспользование между подтемами ограничено только форматно-техническим слоем: шаблонами структуры, автоматическими пакетами, `web-plan`, каскадами источников, локальными валидаторами, сборкой, метриками и иными служебными механизмами без переноса substantive-содержания.
- Если для экономии токенов агент использует ранее подготовленный материал, он может опираться только на перечни источников, формат карточек, технические заготовки и локальные скрипты, но не на готовые аналитические абзацы, мини-конспекты, дельта-аудит, выводы и иные содержательные блоки другой подтемы.

## Протокол Жесткого Lean-Mode

- После стартовой команды пользователя по подтеме агент работает в режиме `silent end-to-end`.
- Формула исполнения обязательна: `один короткий старт -> один тихий основной прогон -> один финальный ответ`.
- После старта агенту запрещено писать в чат до финала, если нет реального блокера.
- Реальный блокер только один из следующих:
  1. нужно решение пользователя по конфликту правил;
  2. требуется внешнее разрешение на действие вне sandbox;
  3. найден риск повреждения данных;
  4. обнаружено прямое противоречие канона, которое нельзя снять самостоятельно.
- Не считаются блокером и не дают права писать в чат:
  1. промежуточные метрики;
  2. частичная готовность;
  3. локальные улучшения;
  4. исправление собственных ошибок;
  5. замечания пользователя, не требующие выбора;
  6. технические штатные действия;
  7. желание доложить прогресс.
- Любое замечание пользователя, не содержащее нового выбора, агент обязан молча учесть и немедленно продолжить тот же прогон без остановки.
- Все штатные действия выполнять молча: поиск, добор документов, правки run-файлов, trust-исправления, assemble, metric-check, page-check, publish, post-publish check.
- Метрики проверять только в конце substantive-добора релевантных документов со структурным элементом, кроме случаев явной технической диагностики.
- Publish выполнять только после полного substantive-добора релевантных документов со структурным элементом и финальной сборки.
- Если publish заблокирован технически, агент молча устраняет блокировку и продолжает, не останавливая прогон.
- Добор при недоборе выполнять только крупными батчами новых релевантных документов со структурным элементом.
- Серия мелких дозаписей, множественные промежуточные сборки и частые сообщения в чат считаются нарушением lean-mode.
- Любая остановка без реального блокера считается дефектом исполнения.
- Нарушение `silent end-to-end` презюмируется как перерасход токенов и как неисполнение AGENTS.md.

## Жесткие Запреты Отклонения И Орchestration-Budget

- Базовый рабочий потолок orchestration-расхода для одной темы или подтемы составляет не более `3%`, если пользователь в текущей инструкции не задал более строгий лимит.
- Если пользователь задал более строгий лимит, действует более строгий лимит.
- Агенту запрещено расширять объем работы сверх буквального запроса пользователя под предлогом надежности, полноты контура, удобства, привычного workflow или собственной перестраховки.
- Агенту запрещено смешивать substantive-задачу и housekeeping в одном прогоне. Архивирование, cleanup, перенос старых run, пересортировка служебных папок и иные побочные действия выполняются только по прямой команде пользователя или отдельным самостоятельным прогоном.
- Агенту запрещено запускать новый `run-subtopic`, если пользователь просит частичный redo внутри уже существующей подтемы. В таком случае использовать только теплый существующий run.
- Для частичного redo агент обязан работать в режиме `surgical-redo`: разрешены только явно заказанные части и только минимально необходимые для них команды.
- В режиме `surgical-redo` агенту запрещено:
  - трогать незаказанные Части;
  - перечитывать целиком неизменившиеся канонические файлы;
  - пересобирать общие dynamic packets, если они не нужны для явно заказанных Частей;
  - запускать auto-assemble и publish до прямой команды пользователя;
  - выполнять archive/cleanup/housekeeping;
  - оправдывать отклонение тем, что так "безопаснее" или "надежнее".
- Для задач вида `redo only Part X/Y/...` отклонение от буквального набора запрошенных Частей считается дефектом исполнения даже тогда, когда substantive-результат формально получен.
- Любой перерасход orchestration сверх действующего лимита при сохранении того же substantive-объема считается дефектом исполнения и требует исправления траектории, а не объяснений пользователя.

## Правила Насыщенности И Стоп-Критерии

- Требование "содержательный и юридически конкретный" считается не пожеланием, а обязательным критерием приемки результата.
- Агент не имеет права останавливаться, если сам видит, что результат еще слабый, неполный, недочищенный или уступает канону проекта по качеству.
- Если агент сам выявил дефект, он не докладывает о проблеме как о промежуточном результате и не ждет новой команды пользователя, а автоматически продолжает рабочий цикл до исправления.
- Добор допустим только за счет новых качественных релевантных документов, новых правовых узлов и содержательного усиления по теме.
- Добор за счет воды, повторов, декоративного объема, формального растягивания текста и нерелевантных документов запрещен.
- Запрещено включать документ в карточку если его связь с подтемой сводится к одной формальной фразе (например «документальное оформление по корпоративным спорам», «по спорам на водном транспорте»). Документ принимается только если его конкретная статья или раздел прямо регулирует предмет подтемы, а поле «Структурный элемент» содержит строго релевантную статью или раздел — не общую ссылку на закон целиком. Если реально найденных релевантных документов меньше целевого ориентира — честно указать фактическое число; добор нерелевантными документами запрещён даже при угрозе недобора метрики.
- Для каждого документа агент обязан поднимать новый `run` и вести его как end-to-end прогон: один сильный проход с целевым итогом сразу, без промежуточной недовыдачи.
- Правильная модель работы: не `собрать что есть и посмотреть, сколько страниц выйдет`, а `сразу гнать подтему до насыщенного финала, пока не получится сильный документ нужного класса`.
- Для каждого документа обязательный рабочий цикл таков:
  1. Подтвердить новый `run` и проверить его пустое состояние.
  2. С нуля подготовить новый корпус Частей `2–5`.
  3. С нуля подготовить новый корпус Частей `6–11`.
  4. Захватить новые части в `run` и собрать финальный `.md/.docx`.
  5. Проверить объем и при необходимости продолжить добор до сильного финала.
- Агенту запрещено публиковать финальный файл, если по внутренней проверке материал выглядит как формально корректный, но узкий, пересушенный или бедный по документной опоре по сравнению с сильными образцами проекта.
- Для подтем данного класса целевым ориентиром финального `.docx` является диапазон `70–80` страниц, если сама тема не требует объективно большего объема.
- Агент обязан проверять не только оценочные метрики текста, но и фактическое число страниц финального `.docx` в рендере документа.
- Если фактический page count заметно ниже рабочего диапазона `70–80` страниц, это презюмируется как дефект исполнения, пока агент не докажет обратное через объективно исчерпанный корпус релевантных материалов.
- Перед финальной публикацией агент обязан проводить `density-gate` по трем осям: `ширина охвата`, `количество и разнообразие опорных документов`, `плотность аналитики`.
- Требуемый объем страниц должен достигаться исключительно за счет `ширины охвата`, `количества и разнообразия опорных документов` и `плотности аналитики`, а не за счет искусственного раздувания оформления, пустых повторов, растянутых интервалов, декоративных разрывов или механического многословия.
- Если объем страниц не достигнут именно из-за недобора по ширине охвата, опорным документам и плотности аналитики, это безусловный `FAIL`.
- `Ширина охвата` означает, что даже узкая подтема должна проверяться не только по прямой норме, но и по смежным слоям: профильный материальный закон, процессуальный слой, подзаконные акты, разъяснения, методические документы, цифровой слой, специальный субъектный слой, международный слой, практические основания отказа/оспаривания/обжалования, редакционные изменения.
- `Количество и разнообразие опорных документов` означает, что агент не вправе останавливаться на минимальном ядре из нескольких очевидных актов, если тема объективно тянет письма, методические рекомендации, приказы, регламенты, разъяснения органов, региональные акты и смежные кодексы.
- При поиске и нахождении документов со структурным элементом агент обязан ориентироваться не только на слова, знаки, страницы и количество ссылок, но и на содержательные показатели насыщенности: число уникальных и релевантных нормативно-правовых актов, ширину правового охвата, наличие судебного слоя, наличие методических и разъяснительных документов, наличие практических примеров и пригодность материала для реальной нотариальной работы. Если документ формально достигает количественных метрик, но показывает недостаточную насыщенность по числу уникальных нормативных актов, разнообразию правовых слоев, наличию судебного слоя, наличию методических и разъяснительных документов, наличию практических примеров или практической полезности для нотариальной работы, результат считается недостаточно сильным и подлежит дальнейшему документному добору.
- `Плотность аналитики` означает, что Части `2`, `5`, `6–10` должны содержать не только перечисление документов, но и разворачивание правовых узлов: как норма работает, что именно проверяет нотариус, где возникает риск ошибки, как считается тариф, когда меняется состав действий, когда возможен отказ, какие документы подтверждают вывод.
- Если по сравнению с сильным внутренним образцом проекта итог явно уступает по насыщенности, агент обязан считать это дефектом исполнения и продолжать добор без отдельного напоминания пользователя.
- Для тарифных подтем агент обязан специально расширять корпус за пределы голой тарифной формулы: предмет нотариального действия, база взимания, состав юридически значимых действий, федеральный и региональный слой, льготы, ограничения, случаи неприменения, смежные методические и организационные акты, практические сценарии расчета, риски ошибочного исчисления, основания для проверки и возможного спора.
- Для Части `10` мини-конспект не считается принятым, если он выглядит как краткое резюме. Он должен быть опорным учебно-практическим конспектом, который можно использовать для подготовки стажера к реальной работе по подтеме.
- Для Частей `6–9` результат не считается принятым, если follow-up фактически подтвердил уже найденное, но не расширил картину по органам, специальным актам, смежным режимам или редакционным изменениям там, где такой материал объективно существует.
- Части `6–9` не считаются завершенными, пока не убраны повторы, не выполнен дельта-аудит и не пройден каскад `7` источников по мастер-промпту с добавлением только новых релевантных карточек.
- Агенту запрещается предоставлять повторяющиеся документы как новый результат последующих Частей; запрещается повторное раздувание корпуса документов одними и теми же актами.
- Для Частей `6–9` результат считается слабым и не подлежит публикации, если в нем нет нового документного материала, найденного через обязательный 7-каскадный поиск по мастер-промпту.
- Агент обязан сравнивать промежуточный итог не только с форматом Приказа, но и с лучшими плотными артефактами проекта. Если текущий материал заметно слабее эталона по насыщенности, это означает не "тема уже", а необходимость добора до разумного предела по данной подтеме.
- Агенту запрещено оправдывать слабый итог ссылкой на то, что "контур соблюден". Соблюдение контура без достаточной содержательной массы считается неполным исполнением.
- Если после сборки `.md/.docx` итоговый документ визуально или по метрикам выглядит аномально коротким для данного класса подтем, агент обязан до публикации вернуться в рабочий цикл и усилить соответствующие Части, а не объяснять недобор уже после жалобы пользователя.
- Если итоговый `.docx` не достигает целевого диапазона страниц и при этом недобор связан с узким охватом, бедным набором опорных документов или слабой аналитикой, результат должен маркироваться как `FAIL` во внутренней приемке и возвращаться на повторный цикл исполнения до устранения дефекта.
- В ходе подготовки подтемы агент обязан вести промежуточный `metric-gate` по рабочему `.md`: число слов, число знаков, число упоминаний `URL1`, число упоминаний `URL2`, масса карточечного корпуса и примерный прогноз итогового page count.
- Перед публикацией агент обязан проходить финальный `page-gate` по готовому `.docx`: проверять фактический page count уже после сборки документа в рендере, а не подменять эту проверку длиной `.md`, числом слов или размером файла.
- Если метрики или page count не достигают рабочего диапазона, агент обязан возвращаться не к абстрактному “добору содержания”, а к добору через новые качественные релевантные документы, новые карточки и новые практические полезные правовые узлы, подтвержденные документами.
- Добор под метрики за счет пустого расширения аналитики без нового документного материала запрещен.
- Если не хватает насыщенности, агенту запрещается расширять текст по тем же самым актам; он обязан идти в новый поиск и приносить новые релевантные документы.
- Постоянные базовые ориентиры по метрикам и page count фиксируются в `AGENTS.md` и `PROJECT_STATE.md` как проектный стандарт, а пользователь может дополнительно задавать или ужесточать их в текущей инструкции для конкретной темы или подтемы.
- Для каждой новой подтемы агент обязан выполнять `metric-check` минимум дважды: после сборки Частей `2–5` и после сборки Частей `6–11`, до финальной публикации.
- В каждый `metric-check` агент обязан фиксировать минимум: слова, знаки, количество строк `URL1`, количество строк `URL2`, количество строк `VERIFIED URL2`, а также краткий вывод о достаточности или недоборе по отношению к целям текущей подтемы.
- После первой сборки готового `.docx` агент обязан выполнять отдельный `page-check`; если page count ниже целевого диапазона, публикация запрещена до нового документного добора и повторной сборки.
- Если пользователь в текущей инструкции задал специальные метрики для темы или подтемы, они имеют приоритет над общими рабочими ориентирами проекта.
- При недоборе метрик по словам агенту запрещается добирать объем словами: запрещены расширение текста по тем же актам, повторный пересказ, риторическое растягивание, декоративное уплотнение аналитики и иные формы добора без нового документного материала. Добор допускается только через новый поиск и включение новых релевантных документов со структурным элементом по текущей подтеме. Повторное раздувание корпуса уже использованными документами запрещено.

## Правила Метрик Во Время Работы

- После каждого `capture-part-output` запускать `metric-check --target master` и фиксировать текущие `words`, `url1`, `url2`.
- Пороги публикации: `words ≥ 16000`, `url1 ≥ 100`, `url2 ≥ 100`.
- Если после захвата Части 7 метрики ниже 60% порога — увеличить глубину поиска в Частях 8–9.
- Если после захвата Части 10 метрики не достигнуты — выполнить дополнительный поисковый раунд (дельта-аудит) перед сборкой, не ждать `assemble`.
- Не откладывать проверку метрик на момент публикации.

## Правила Ссылок И Верификации

- Анкерные ссылки интерфейса не считаются итоговым результатом.
- Итоговым результатом считаются только копируемые текстовые ссылки.
- При поиске и верификации документов агент обязан использовать каскад источников по мастер-промпту в следующем порядке:
  1. `КонсультантПлюс` — `https://www.consultant.ru/`
  2. `Гарант` — `https://base.garant.ru/`
  3. `Норматив/Контур` — `https://normativ.kontur.ru/`
  4. `Legalacts` — `https://legalacts.ru`
  5. `Rulaws` — `https://rulaws.ru`
  6. `Официальный правовой портал` — `https://xn--h1apee0d.xn--p1ai`
  7. Далее — иные источники только при необходимости.
- Этот каскад обязателен как порядок первичного поиска, проверки читабельного `URL2` и добора альтернативных карточек, если верхние уровни каскада не дали достаточного результата.
- Переход к следующему уровню каскада допустим, если предыдущий источник не дал применимого документа, не дал подтверждаемого структурного элемента, не дал читабельного `URL2` или не позволил надежно сверить реквизиты.
- Использование более позднего источника без попытки пройти более ранние уровни каскада считается нарушением рабочего режима, если не доказана объективная невозможность такой проверки.
- Все названные в ответе документы должны оформляться карточками: полное наименование, структурный элемент, URL1, статус или ссылка URL2 и иные поля верификации по применимости.
- `URL1` и `URL2` не являются дублем: `URL1` = якорь официальности, `URL2` = читабельная проверенная ссылка.
- В строках `VERIFIED URL2`, `Заголовок страницы URL2`, `Сверка реквизитов` URL писать нельзя.
- При слабом или неподтвержденном `URL2` документ не должен теряться, если уже подтверждены его существование, применимость, роль и `URL1`.
- `КАРАНТИН` применяется только при материальной неопределенности по самому документу: существование, действие, точная идентификация, применимость или структурный элемент.
- Если документ остается в `A` или `B` при слабом `URL2`, это должно сопровождаться явным статусом `VERIFIED URL2` и краткой сверкой реквизитов без вывода самих URL в этих строках.
- Поле `VERIFIED URL2` обязано быть **отдельной строкой** в карточке документа в точном формате: `VERIFIED URL2: ДА` или `VERIFIED URL2: НЕТ`. Запрещено встраивать его в другое поле (например `Статус верификации: VERIFIED URL2: ДА` или `Статус верификации: VERIFIED URL2=ДА`). Скрипт считает только строки начинающиеся с `VERIFIED URL2:` — любой другой формат даёт счётчик 0. Эталонная карточка выглядит так:

```
Вид документа: Федеральный закон
Полное наименование: Федеральный закон от 11 февраля 1993 г. N 4462-1 «Основы законодательства Российской Федерации о нотариате»
Орган принятия: Верховный Совет РФ
Дата и номер: 11.02.1993 N 4462-1
Структурный элемент: Статья 103. Обеспечение доказательств
Статус: действует
VERIFIED URL2: ДА
URL1: `http://www.pravo.gov.ru/proxy/ips/?docbody=&nd=10102426`
URL2: `https://www.consultant.ru/document/cons_doc_LAW_1581/`
Заголовок страницы URL2: Основы законодательства Российской Федерации о нотариате
Сверка реквизитов: дата/номер/полное название — совпало
Значение для темы: [конкретное содержательное объяснение — см. правило ниже]
```

Поле `VERIFIED URL2:` стоит ПЕРЕД `URL1` и `URL2` и является самостоятельной строкой без какого-либо префикса.


## Правила Выхода

- Готовые результаты сохранять в `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\output ready`.
- Результат должен быть подготовлен в `.md` и `.docx`.
- Формат результата должен соответствовать утвержденным образцам.
- Финальный файл должен формироваться максимально близко к прямому результату staged-взаимодействия с LLM.
- Постобработка допускается только минимальная: сборка по Частям, техническая склейка блоков и перенос в `.md/.docx`.
- Агент не должен переписывать качественно готовые блоки заново, если они уже соответствуют принятому формату и могут быть вставлены в финальный документ как есть.
- Финальный документ должен выводить явные разделители `ЧАСТЬ 2` ... `ЧАСТЬ 11`; служебные Части I-III в финальный файл не публикуются.
- В карточечных разделах документы нумеруются, чтобы итог не шел сплошным текстом.
- В итоговом `.md` ссылки публикуются в эталонном читабельном виде: `URL1`, `URL2`, `VERIFIED URL2`, `Заголовок страницы URL2`, `Сверка реквизитов`, без принудительного заключения всех ссылок в fenced code blocks.
- В `.docx` эти же поля выводятся как структурированные моноширинные блоки без markdown-маркеров fenced code blocks.
- Если документ назван в аналитике, Карантине, FAIL-SAFE CHECK или в поиске по органам, он не может оставаться без карточки с полным наименованием, структурным элементом и ссылочным блоком.
- Поиск по органам власти публикует не сгруппированные строки `Прямой документ(ы): ...`, а отдельные карточки документов, привязанные к соответствующему органу.
- Канонические итоговые файлы именуются по схеме `номер подтемы + краткий заголовок из Оглавления`.
- Краткий заголовок для имени файла берется из утвержденной формулировки соответствующей конечной подтемы и не придумывается заново агентом.
- После завершения одной основной темы агент обязан остановиться и ждать подтверждения пользователя перед переходом к следующей теме.

## Правила Репозитория

- В Git-репозиторий публикуется только сам агент и его постоянная проектная инфраструктура: код, `AGENTS.md`, `PROJECT_STATE.md`, workflow-инструкции, шаблоны, исходные промпты, образцы, конфиги, скрипты и иные исходные файлы, необходимые для воспроизводимой работы агента.
- Любые результаты конкретных прогонов в Git запрещены: `run`-папки, staged outputs, служебные сборки, финальные выгрузки в `input\output ready`, временные файлы, промежуточные сборки, `fresh_*`, `tmp`, `regen_*` и иные generated artifacts.
- Папки публикации и исполнения проекта могут существовать локально как рабочая среда, но не считаются частью Git-состава репозитория.
- Перед `git add`, `commit` и `push` агент обязан отдельно проверить, что в индекс не попали generated artifacts и иные производные результаты конкретного исполнения.
- Базовое правило репозитория: в Git хранится только воспроизводимая система агента; все артефакты отдельных прогонов и публикационные выгрузки хранятся локально вне Git.

## Правила Архива Run-Артефактов

- Старые `run`-артефакты не должны подолгу оставаться в активной рабочей папке, если они уже не нужны для текущего исполнения подтемы.
- После завершения подтемы и проверки канонической публикации старые run-артефакты должны по возможности выводиться из активного рабочего контура в архивную папку вроде `runs_archive`, чтобы не засорять контекст и файловую среду.
- Агент не должен без необходимости читать старые run-артефакты из архивного массива и не должен подтягивать их в текущий контекст, если они не нужны для конкретной проверки, восстановления или аудита.
- Рабочее правило: в активной папке держать только текущий run и минимально необходимый набор актуальных рабочих файлов; исторические run-артефакты уводить в архив.

## Правила Состояния

- После важных изменений обновлять `PROJECT_STATE.md`.
- Если утвержден новый outline override, это должно быть отражено в `PROJECT_STATE.md`.
- Если логика работы изменилась на постоянной основе, обновлять `AGENTS.md`, а не только переписку.

## Техническое Правило Патчей

- Если агент записывает длинный текстовый блок в файл через `apply_patch`, он не должен сначала пытаться провести весь массив одним сверхкрупным патчем.
- Для больших частей, карточечных массивов и длинных финальных блоков агент обязан по умолчанию писать файл порционно: сначала создать файл или вставить первую секцию, затем последовательно дозаписывать следующие секции отдельными патчами.
- Это правило применяется прежде всего к длинным Частям `3`, `4`, `10`, а также к любым иным крупным блокам, где единый патч повышает риск технического сбоя.
- Порционная запись считается базовым режимом для больших файлов и используется для ускорения работы, снижения числа технических отказов и сохранения стабильности `run`.
- При порционной записи агент обязан избегать создания параллельных дублей одного и того же большого текста в нескольких временных файлах без необходимости.

## Правило Чат-Шума

- Агенту запрещено засорять чат сообщениями о штатных технических дроблениях, ретраях, мелких обходах, служебных перепроверках и иных внутренних действиях, если они не создают блокер для результата.
- Сообщения о техническом исполнении допустимы только при реальном блокере, конфликте правил, необходимости решения пользователя или when the result cannot be safely delivered without такого сообщения.
- Нормальный технический обход, штатное дробление записи и локальная механическая проверка считаются внутренним слоем исполнения и не должны превращаться в отдельный чат-шум.

## Блокирующий Режим Исполнения

- Перед началом каждой подтемы агент обязан:
  - сначала перечитать и кратко внутренне сверить Мастер-Промпт;
  - затем перечитать и кратко внутренне сверить Приказ по текущей подтеме;
  - после этого работать только по их буквальному контуру, без самодельных сокращений, переименований и "оптимизаций".
- Агент обязан экономить токены:
  - не пересказывать пользователю заново Мастер-Промпт и Приказ;
  - не дублировать их длинными сводками;
  - использовать их как уже известный канон исполнения;
  - обращаться к ним точечно только для проверки структуры, названий блоков, формата и обязательных элементов.

## PRE-PUBLISH GATE

- Публикация в `04-output` запрещена, пока не пройдены все проверки:
  - все Части соответствуют Приказу дословно по структуре, в том числе структуре образца финального файла `C:\Users\koper\Downloads\GitHub Projects\Notary Qwen\input\output examples\15.11.10. Наследование имущества.md`;
  - в Частях `3`, `4`, `5` все найденные документы оформлены карточками;
  - у каждого упомянутого документа есть:
    - полное наименование;
    - структурный элемент;
    - `URL1`;
    - `URL2`;
    - `VERIFIED URL2`;
    - `Заголовок страницы URL2`;
    - `Сверка реквизитов`;
  - в Части `10` после каждого смыслового пункта есть документы-опоры и ссылки;
  - в Части `11` нет документов, только действия стажера;
  - названия слоев, блоков и разделов не переименованы агентом;
  - имя финального файла соответствует краткому заголовку из Оглавления;
  - выполнена проверка метрик текста;
  - выполнена проверка `page count` `.docx`.
- Если хотя бы один пункт не пройден, агент не имеет права публиковать результат и обязан молча продолжать до исправления.

## Запрет Ранней Публикации

- Пользователь не должен быть первым валидатором базовых дефектов.
- Если пользователь находит структурный, форматный или ссылочный дефект, это считается ошибкой исполнения агента.
- Агент обязан сначала сам отловить такие дефекты через `PRE-PUBLISH GATE`.

## Каноническая Публикация

- Канонический финальный файл всегда публикуется только в `04-output`.
- Папка `05-runs\...\03-final` содержит только служебную сборку run и не считается местом финальной публикации.
- Завершенной подтема считается только после публикации канонических файлов в:
  - `04-output\md`
  - `04-output\docx`
- Канонические файлы публикуются только с кратким заголовком из Оглавления.

## Разделение Ролей: LLM vs notary_agent.py

- `notary_agent.py` **никогда не генерирует substantive-текст**. Он — оркестратор: создаёт workspace и пакеты, валидирует результат через `capture-part-output`, собирает финал и публикует.
- **LLM (Qwen)** — единственный генератор substantive-текста: поиск документов через web_search/web_fetch, карточки документов, аналитика, мини-конспект, практические задания.
- **Человек** — запускает команды оркестратора, проверяет результат, даёт команды на продолжение.
- Правильный workflow: `prepare-part-XX-plan` → LLM генерирует текст Части → `capture-part-output` → повторить для каждой Части → `assemble-subtopic-final`.
- LLM **запрещено** писать собственные скрипты конвертации в обход `capture-part-output`.
- LLM **запрещено** утверждать, что `notary_agent.py` сам ищет документы или генерирует текст — это ложь.
- LLM **запрещено** говорить «передаю управление агенту» — это бессмысленно, агент текст не генерирует.
- LLM **запрещено** создавать Python-скрипты (`*.py`) для генерации карточек документов, текста Частей или списков URL. Любой `.py` файл в `tmp/` или рядом, генерирующий substantive-контент — признак обхода правил; прогон считается скомпрометированным и подлежит повторению с нуля.
- LLM **запрещено** заранее готовить контент для будущих подтем в `tmp/` или любой другой папке. Каждая подтема начинается с чистого нового `run-subtopic` и реального поиска.
- Карточки документов создаются **только** через реальный `web_search`/`web_fetch` с открытием страницы. URL2 вписывается только после того, как страница реально открыта и реквизиты документа подтверждены визуально. Подбор URL из памяти модели запрещён.
- **`web_search` обязателен для каждого документа в Частях 2–11.** Включение документа в карточку без предшествующего вызова `web_search` запрещено. Каждый реальный поисковый запрос должен быть зафиксирован в `04-web-plan/research-log.jsonl`. Пустой `research-log.jsonl` при захвате Части — признак отсутствия реального поиска; скрипт заблокирует `capture-part-output` с ошибкой.
- **`web_fetch` обязателен перед каждым URL2.** Каждый документ проходит трёхшаговый цикл:
  1. `web_search` → получил URL-кандидат
  2. `web_fetch URL` → открыл страницу, увидел реальный заголовок
  3. **ТОЛЬКО ТЕПЕРЬ** вписываю URL2 в карточку, заголовок страницы — в `Заголовок страницы URL2`
  Запрещено вписывать URL2 без предшествующего `web_fetch` этой конкретной ссылки. Если между `web_search` и карточкой нет вызова `web_fetch` для этого URL — карточка нелегитимна.
- **Запрет пакетной генерации карточек.** Генерация 10+ карточек подряд без промежуточных `web_fetch` — признак подстановки URL из памяти модели. Каждая карточка должна быть подтверждена отдельным вызовом `web_fetch`.
- **Если `web_fetch` не совпал** (страница открылась, но документ другой) — НЕ вписывать этот URL2. Пометить карточку: `Статус верификации: UNVERIFIED`, `URL2: отсутствует`, краткая причина в `Сверка реквизитов`.
- **MISMATCH при автоматической проверке** — это не «блокировка», а автоматическая санация: `sanitize_mismatch_url2` удаляет битый URL2 из финального файла. Но это — последняя линия защиты; первичная ответственность лежит на LLM, который обязан проверять каждый URL2 через `web_fetch` до записи в карточку.

## Статусы URL2 при автоматической проверке (verify-url2)

- `OK` — страница открылась, заголовок совпал с заявленным. Публикация разрешена.
- `MISMATCH` — страница открылась, но заголовок **не совпадает** с заявленным документом. URL2 ведёт не на тот документ. При сборке вызывается `sanitize_mismatch_url2` — битый URL2 удаляется из финального файла. Первичная ответственность — на LLM: каждый URL2 обязан быть проверен через `web_fetch` до записи в карточку.
- `UNVERIFIED` — страница недоступна автоматически (403, таймаут, сеть). Публикацию **не блокирует**, но LLM обязан проверить каждую такую ссылку вручную (`web_fetch`) и подтвердить или исправить. Список UNVERIFIED-карточек выводится в отчёте с `Полным наименованием` и `Структурным элементом`.

## Сессия 04.04.2026 — Системные ошибки при публикации 16.9.1

### Что произошло

Пользователь сказал «публикуй как есть» при недостигнутых метриках. LLM вместо `--force` начал цепочку самолечения, которая последовательно ухудшала ситуацию:

1. Удалил `manifest.json` → все части стали `unknown`, публикация заблокирована полностью.
2. Запустил regex-фикс по `tmp\`-файлам без бэкапа → файлы Parts 4–10 потеряли все строки URL2.
3. Продолжал чинить следствия собственных ошибок вместо того чтобы остановиться.
4. Задавал вопрос «Продолжаю?» вместо исполнения уже данной инструкции.

Итог: пользователь опубликовал финальный файл вручную сам.

### Правила — никогда не повторять

**Правило 1. `manifest.json` не трогать.**
Это реестр доверия run'а. Удаление переводит все части в статус `unknown` и блокирует публикацию полностью. Никаких исключений.

**Правило 2. Перед patch/regex на source-файлах — сделать копию.**
Сначала `copy file.md file.bak.md`, потом чинить. Без бэкапа — не трогать.

**Правило 3. «Публикуй как есть» = `--force`, без обсуждения.**
Если пользователь принял решение публиковать при недостигнутых метриках — исполнять `--force`. Не предлагать альтернативы, не чинить контент.

**Правило 4. Цепочка самолечения запрещена.**
Если один шаг не прошёл — остановиться, доложить пользователю состояние, ждать команды. Не продолжать исправлять последствия собственных ошибок новыми действиями.
